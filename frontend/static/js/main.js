// static/js/main.js

document.addEventListener("DOMContentLoaded", () => {
    // The DOM is fully loaded.
    console.log("F1 Data Explorer Home Page Initialized.");
    
    // Future Home Page Logic:
    // If you and your partner decide to put a "Summary Dashboard" chart 
    // on the home page (like total races per year), you would fetch 
    // that API route here and pass it to a D3 function.
});